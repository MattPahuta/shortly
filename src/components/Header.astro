---
import { Image } from "astro:assets";


import logo from '../../src/images/logo.svg';
import hamburger from '../../src/images/icon-hamburger.svg';
import close from '../../src/images/icon-close.svg'
---

<header class="">

  <div class="container mx-auto flex gap-8 items-center max-w-6xl py-6 px-6 xl:px-0">
    <div class="topnav">
      <Image class="" src={logo} alt="shortly" />
      <nav>
        <span id="nav-label" hidden>Navigation</span>
        <button 
          id="btnOpen" 
          class="topnav__open md:hidden"
          aria-expanded="false"
          aria-labelledby="nav-label">
          <Image src={hamburger} alt="" class="size-7" />
        </button>
        <div class="topnav__menu z-20" role="dialog" aria-labelledby="nav-label">
          <button id="btnClose" class="topnav__close md:hidden" aria-label="Close">
            <Image src={close} alt="" class="size-7"/>
          </button>
          <ul class="topnav__links uppercase font-thin text-2xl text-white md:text-base md:font-alata md:normal-case md:font-normal">
            <li class="topnav__item group">
              <a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Features</a>
            </li>
            <li class="topva__item group">
              <><a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Pricing</a>
            </li>
            <li class="topva__item group">
              <a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Resources</a>
            </li>
          </ul>
        </div>
        <div class="lg:flex items-center space-x-6 lg:text-base font-bold text-grayishViolet">
          <a href="#" class="hover:text-veryDarkViolet">Login</a>
          <a href="" class="px-8 py-3 font-bold text-white bg-cyan rounded-full hover:bg-cyanLight ease-in-out duration-300">Sign Up</a>
        </div>
      </nav>

    </div>
  </div>


  <!-- <div class="container mx-auto flex gap-8 items-center max-w-6xl py-6 px-6 xl:px-0">
    <Image class="" src={logo} alt="shortly" />
    <nav id="menu" class="flex items-center justify-between w-full">
      <ul class="flex space-x-8 lg:text-base font-bold">
        <li><a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Features</a></li>
        <li><a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Pricing</a></li>
        <li><a href="#" class="text-grayishViolet hover:text-veryDarkViolet">Resources</a></li>
      </ul>
      <div class="lg:flex items-center space-x-6 lg:text-base font-bold text-grayishViolet">
        <a href="#" class="hover:text-veryDarkViolet">Login</a>
        <a href="" class="px-8 py-3 font-bold text-white bg-cyan rounded-full hover:bg-cyanLight ease-in-out duration-300">Sign Up</a>
      </div>
    </nav>
  </div> -->
</header>

<style>
/* Navigation Styles */
.topnav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.topnav__links {
  display: flex;
  gap: 2rem;
  align-items: center;
}

@media (max-width: 48em) {
  .topnav__menu {
    position: fixed;
    inset: 0;
    padding: 3rem 1.5rem;
    background-color: black;
    translate: 100vw 0;
    transition: translate 0.5s ease-in-out;
  }
  .topnav__links {
    flex-direction: column;
    align-items: start;
  }
}

.topnav__close {
  display: block;
  margin-inline-start: auto;
  margin-bottom: 8rem;
}

.topnav__open[aria-expanded=true] + .topnav__menu {
  translate: 0;
}

@media (prefers-reduced-motion) {
  .topnav__open[aria-expanded=true] + .topnav__menu {
    opacity: 1;
  }
}

/* handle menu animation for users w/animations disabled */
@media (width < 48em) and (prefers-reduced-motion) {
  .topnav__menu {
    translate: 0;
    opacity: 0;
    transition: opacity 500ms ease-in-out;
  }
}
</style>